# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release
*.local
*.tsbuildinfo

# Editor directories and files
.vscode/.debug.env
.idea

# Claude Code local state and git worktrees (never commit — a committed
# worktree dir becomes a bare gitlink that breaks CI's checkout)
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# lockfiles are managed outside PR review
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock
/test-results/
/playwright-report/
/playwright/.cache/

test/screenshots

graphify-out